# Calculating the area of the traingle 

base = float(input("Enter the length of base "))

height = float(input('Enter the length of the height '))

area = 0.5 * base * height 

print("The area of the triangle is \n", area) 