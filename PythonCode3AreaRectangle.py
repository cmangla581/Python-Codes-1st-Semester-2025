# Calculating the area of hhe rectangle 

length = float(input('Enter the length '))

breadth = float(input("Enter the breadth "))

area = length * breadth 

print(f"The area of the rectangle is {area}")